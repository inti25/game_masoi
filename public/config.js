
const renderOptions = {
    width : 320,
    height : 568
};

const SETTINGS = {
    DEBUG : false
};

window.SETTINGS = SETTINGS;

const assetsBaseUrl = "./../assets";

export default {
    renderOptions,
    assetsBaseUrl,
    settings : SETTINGS
};
